mutation Login($name: String!, $password: String!) {
  login(userFields: { name: $name, password: $password }) {
    errors {
      field
      message
    }
    user {
      ...RegularUser
    }
  }
}
