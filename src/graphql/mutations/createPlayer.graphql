mutation CreatePlayer($name: String!, $password: String!, $email: String!) {
  createPlayer(
    userFields: { name: $name, password: $password, email: $email }
  ) {
    errors {
      field
      message
    }
    player {
      ...RegularPlayer
    }
  }
}
